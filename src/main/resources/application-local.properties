# Settings for local(dev)
web-scraping.server.url=${LOCAL_SCRAPING_SERVER_URL}

spring.config.activate.on-profile=local

spring.datasource.url=${EATERY_LOCAL_DB_URL}
spring.datasource.username=${EATERY_LOCAL_DB_USERNAME}
spring.datasource.password=${EATERY_LOCAL_DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
